const initialState = "red";

// create reducer
const backgroundColor = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// export defoult
export default backgroundColor;
